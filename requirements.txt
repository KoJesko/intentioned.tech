# Intentioned - Python 3.12 Requirements
# Last updated: January 2026
# Note: Python 3.12 required for Kokoro TTS and NeMo/Parakeet STT
# Core Framework
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.10.0
starlette>=0.41.0
websockets>=14.0
python-multipart>=0.0.12

# PyTorch (CUDA 12.8) - Use latest compatible versions
--extra-index-url https://download.pytorch.org/whl/cu128
torch>=2.7.0
torchaudio>=2.7.0
torchvision>=0.22.0

# Speech-to-Text (STT)
# Primary: NVIDIA Parakeet TDT 0.6B (best accuracy, zero hallucination)
nemo_toolkit[asr]>=2.0.0
# Fallback: Vosk (offline, lightweight)
vosk>=0.3.45
# Alternative: Wav2Vec2 via transformers

# Text-to-Speech (TTS)
# Primary: Kokoro 82M (StyleTTS2, Apache 2.0, high quality)
kokoro>=0.9.4
misaki[en]>=0.9.4
# Fallback options
edge-tts>=6.1.0
pyttsx3>=2.90

# LLM & Transformers
transformers>=4.46.0
accelerate>=1.0.0
safetensors>=0.4.0
huggingface-hub>=0.26.0
tokenizers>=0.20.0
sentencepiece>=0.2.0

# NLP & Phonemization
spacy>=3.7.0
phonemizer-fork>=3.3.0
num2words>=0.5.13

# Audio Processing
soundfile>=0.12.0
scipy>=1.14.0
numpy>=1.26.0,<2.0
librosa>=0.10.0
audioread>=3.0.0
resampy>=0.4.0

# Deep Learning Utilities
pytorch-lightning>=2.4.0
lightning>=2.4.0
einops>=0.8.0
omegaconf>=2.3.0
hydra-core>=1.3.0

# Networking
aiohttp>=3.10.0
requests>=2.32.0

# Logging & Utilities
loguru>=0.7.0
tqdm>=4.66.0
pyyaml>=6.0.0
colorlog>=6.8.0
rich>=13.9.0

# Windows-specific
pywin32>=306; sys_platform == 'win32'
comtypes>=1.4.0; sys_platform == 'win32
bitsandbytes>=0.49.1;
